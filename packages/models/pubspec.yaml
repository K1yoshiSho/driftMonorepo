name: models
description: repository for models
publish_to: 'none'
version: 1.0.0+1

environment:
  # sdk: '>=3.1.0-9.0.dev <4.0.0'
  sdk: ">=2.19.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.3

flutter:
  uses-material-design: true